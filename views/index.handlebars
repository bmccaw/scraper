<main>
    <section id="articles">
        {{#each article}}
        <div class="item" data-id="{{id}}">
            <h5><a href="{{article_link}}" target="_blank">{{headline}}</a></h5>
            <img src="{{img_link}}" class="article-img">
            <p class="summary">Summary: {{teaser}}</p>
            {{#unless saved}}
            <button id="{{id}}" class="btn save">Favorite</button>
            {{/unless}}
            {{#if saved}}
            <button id="{{id}}" class="btn save">Unfavorite</button>
            <button data-id="{{id}}" class="btn note">Notes</button>
            {{/if}}

        </div>
        <div id="modal{{id}}" class="modal">
            <div class="modal-content">
                <h4>{{headline}}</h4>
                <div class="notes"><h5>Notes</h5>
                
                    
                    {{#each notes}}
                    <div class="note-item">
                    <p>Name: {{author}}</p>
                    <p>Note: {{body}}</p>
                    <hr>
                    </div>
                    {{/each}}
                    
                    
                </div>
                <div class="row">
                    <form class="col s12" action="/notes/{{id}}" method="POST">
                        <div class="row">
                            <div class="input-field col s12">
                                <input id="name" type="text" name="name">
                                <label for="name">Name</label>
                            </div>
                            <div class="input-field col s12">
                                <textarea id="note" class="materialize-textarea" name="note"></textarea>
                                <label for="note">Add a note...</label>
                                <button class="waves-effect waves-light btn add-note" type="submit">Submit</button>
                            </div>
                        </div>
                    </form>
                </div>
            </div>
            <div class="modal-footer">
                <a href="#!" class="modal-close waves-effect waves-green btn">Close</a>
            </div>
        </div>
        {{/each}}
    </section>

</main>